# 实验与结果（论文草稿）

> 项目：古事（Gushi）· 多分支叙事的一致性维护
> 数据：`Docs/ablation/ablation_results_full_5x5.json` ｜ 报告：`Docs/ablation/ablation_report.md`
> 状态：**草稿**，数据为单轮 5 故事 × 4 档 × 5 段（n=5），投稿前需扩样

---

## 1. 实验设置

### 1.1 研究问题

本文提出「叙事状态表 + 知识图谱」的混合记忆注入，用于缓解长程故事续写中的前后文不一致。实验回答三个问题：

- **RQ1**：记忆注入能否降低续写内容的重复与套路化？
- **RQ2**：状态表与知识图谱两个模块各自贡献如何？
- **RQ3**：二者叠加是否优于单一模块？

### 1.2 消融设计

四档消融，控制变量：所有档位的**初始记忆数据完全相同**，仅切换「是否将记忆注入 Prompt」与「是否增量更新记忆」。

| 档位 | 状态表注入 | 图谱注入 |
|---|:---:|:---:|
| `none`（基线） | ✗ | ✗ |
| `state` | ✓ | ✗ |
| `graph` | ✗ | ✓ |
| `both` | ✓ | ✓ |

### 1.3 数据

5 个故事，覆盖两类叙事结构：

- **多角色多关系**：桃园结义（4 角色）、赤壁之战（4 角色，多势力）
- **单主线/三方博弈**：张骞出使西域、荆轲刺秦、郭子仪单骑退敌

每档连续续写 **5 段**，共 **100 条**生成样本。基础模型 `deepseek-chat`，`max_tokens=700`，风格指令按题材自动生成。

### 1.4 指标

| 指标 | 定义 | 方向 |
|---|---|---|
| 相邻段重复度 | 相邻段落字符 trigram Jaccard 相似度均值 | 越低越好 |
| 角色覆盖率 | 续写中至少被提及一次的角色占比 | 越高越好 |
| Prompt 注入增量 | 相对 `none` 档的 Prompt 字符增量 | 观测量 |
| 记忆规模 | 图谱节点数 / 状态表对象数 | 观测量 |

---

## 2. 结果

### 2.1 主结果

跨 5 个故事的均值 ± 标准差：

| 档位 | 相邻段重复度 ↓ | 角色覆盖率 ↑ | Prompt 字数 | 图谱节点 |
|------|------:|------:|------:|------:|
| `none`（基线） | 0.332 ± 0.307 | 87% ± 18% | 2696 ± 259 | 169.4 ± 109.2 |
| `state` | **0.043 ± 0.022** | 80% ± 30% | 3602 ± 167 | 174.4 ± 111.1 |
| `graph` | 0.254 ± 0.302 | 87% ± 18% | 3709 ± 231 | 200.0 ± 111.2 |
| `both` | 0.177 ± 0.257 | **93% ± 15%** | 5033 ± 549 | 228.0 ± 110.8 |

**对 RQ1**：注入记忆后重复度从 0.332 降至 0.043（`state`），降幅约 **87%**，方向支持"记忆注入抑制套路化"。

**对 RQ2**：两个模块呈现**互补分工**而非冗余——

- **状态表主导重复度控制**：`state` 档 0.043，是所有档位中最低
- **图谱主导角色覆盖**：`both` 档覆盖率 93%，高于任一单模块

**对 RQ3**：`both` 在角色覆盖上最优（93%），但在重复度上（0.177）**劣于** `state`（0.043）。二者叠加并非单调更优，存在权衡。

### 2.2 关键观察：状态表抑制「退化重复」

逐段分析发现一个在聚合均值中被掩盖的现象：**逐字完全相同的相邻段落（相似度 = 1.00）只出现在未注入状态表的档位**。

| 档位 | 出现完全重复段的故事数 |
|---|---|
| `none` | 2 / 5（荆轲刺秦、郭子仪单骑退敌） |
| `graph` | 2 / 5（荆轲刺秦、赤壁之战） |
| `both` | 1 / 5（郭子仪单骑退敌） |
| `state` | **0 / 5** |

典型案例（`none` 档，郭子仪单骑退敌）：相邻段相似度为 `[0.02, 1.00, 1.00, 1.00]` —— 模型在第二段之后陷入逐字复读，后续三段完全一致，生成本质上已经失败。

**这是 `none` 档高均值与大方差的主要来源**：基线并非"稳定地略差"，而是"多数时候尚可、偶尔彻底崩坏"。

**机理解释**：状态表以 `key-value` 的符号化形式显式给出"谁现在在哪、处于什么状态"，为模型提供了明确的前进锚点；缺少该锚点时，模型在长程续写中更容易在原地打转。这与 SCORE 关于"符号化状态追踪优于纯语义检索"的论点一致。

> ⚠️ **证据强度声明**：n=5，该模式**方向明确但未达统计显著**（见 2.3）。"状态表抑制退化重复"目前是**值得进一步验证的假设**，不宜作为已证结论写入。建议扩样至 ≥15 个故事后重新检验。

### 2.3 统计检验

以故事为配对单位，对相邻段重复度做配对 t 检验（df=4，临界值 2.776）：

| 档位 | 平均差值 | t 值 | 显著 (p<0.05) |
|------|------:|------:|:---:|
| `state` | -0.289 | -2.17 | ❌ 否 |
| `graph` | -0.078 | -0.47 | ❌ 否 |
| `both` | -0.155 | -1.54 | ❌ 否 |

**当前样本量下三个对比均未达显著**。主要原因是基线方差极大（0.307，由 2.2 的退化重复导致），少数崩溃样本主导了均值差异。

**这是本实验最大的局限，也是投稿前必须补的工作**：需要扩大样本量、并对每个配置重复多次取均值，以降低方差、提升统计效力。

### 2.4 记忆系统的实际工作状态

| 档位 | 平均图谱节点 | 平均状态表对象 | Prompt 增量 |
|------|------:|------:|------:|
| `none` | 169.4 | 7.0 | — |
| `state` | 174.4 | **19.8** | +906 |
| `graph` | 200.0 | 6.6 | +1001 |
| `both` | 228.0 | 21.2 | +2337 |

- **状态表随续写持续生长**：`state`/`both` 档从 7 个对象增至约 20 个，证明增量更新在真实生成链路中生效
- **图谱持续扩张**：`both` 档平均达 228 节点（基线 169），说明关系抽取在持续工作
- **Prompt 代价**：`both` 档 Prompt 平均 5033 字，较基线增 87% —— 这是 2.1 中权衡的另一面，需在讨论中权衡"信息量 vs 上下文预算"

---

## 3. 讨论

### 3.1 模块互补性的意义

状态表回答"**现在是什么**"（符号化、精确匹配），图谱回答"**跟什么有关**"（关系结构、BFS 邻域）。二者在指标上呈现清晰分工：前者压制重复、后者维持角色在场。这为"混合记忆优于单一记忆"提供了直接的消融证据。

### 3.2 本文相对 SCORE / DOME 的增量：分支感知

本文的关键差异点是 **分支感知（branch-aware）**：

- SCORE 的动态状态追踪与 DOME 的时序知识图谱均面向**单线叙事**，不存在"同一角色在不同分支处于不同状态"的场景
- 古事的多分支结构使该场景天然存在，本文的状态表与图谱均以 `branchId` 为隔离维度

> ⚠️ **当前实验并未验证这一点，这是本稿最大的论证缺口。**
>
> 上述 4 档消融的每个故事都只跑在**单一分支**（`branch_${tag}_${arm}`）上，本质是一个线性续写任务。
> 它验证的是「记忆注入是否有效」，**不是**「分支隔离是否有效」。
>
> 目前支撑分支感知的只有单元测试级证据：`tests/narrative-state-tracker.test.ts`
> （同一角色在两分支状态不同、写 B 分支不污染 A 分支）与 `tests/knowledge-graph.test.ts`
> （同名节点在不同分支是不同节点）。**单元测试能证明机制正确，但不能证明它改善了生成质量。**
>
> 审稿人几乎必然会问："你声称分支感知是核心创新，那么分支感知的实验在哪里？"
> 因此本节必须在补做「跨分支污染」实验（设计见 §4-1）之后，才能作为论文主线成立。

### 3.3 局限

1. **样本量不足**（最主要）：5 故事 × 1 轮，统计检验均不显著
2. **单一基础模型**：仅 `deepseek-chat`，结论的模型泛化性未知
3. **指标偏形式化**：trigram 重复度只能反映表层重复，无法衡量"情节因果是否连贯""人物性格是否漂移"——需引入 LLM-as-Judge 多维打分（角色稳定/事件因果/时间线连续/情感连贯）作为补充
4. **未接入人工评估**：需人工阅读打分与自动指标交叉印证

---

## 4. 下一步

### 4-1【最高优先级】补做「跨分支污染」实验 —— 让主线成立

这是把「分支感知」从**机制正确**提升到**效果可证**的唯一途径，也是本文能否成立的关键。

**设计草案**：

| 项 | 设计 |
|---|---|
| 场景 | 同一故事的同一分叉点，造出两条**设定冲突**的分支：<br>A 分支：刘备与曹操结盟；B 分支：刘备与曹操反目 |
| 续写 | 在 **A 分支**继续续写 5 段（B 分支的事件绝不应出现） |
| 对照 | 2 档 × 2 组：<br>① 分支隔离**开**（现状，状态表/图谱按 `branchId` 隔离）<br>② 分支隔离**关**（人为让检索忽略 `branchId`，模拟 SCORE/DOME 的单线记忆） |
| 指标 | **跨分支污染率**：A 分支续写中引用了 B 分支专属事件/状态的段落占比（由 LLM-as-Judge 或关键词匹配判定）<br>**反向亦然**（B 分支不得引用 A 分支） |
| 预期 | 隔离开启时污染率显著低于关闭时；这正是 SCORE/DOME 结构上做不到的事 |

**样本**：≥5 个故事的冲突分支对 × 2 档 × 5 段 = 100 条，与主实验同量级。

**为什么这是必须补的**：主实验（4 档消融）证明的是"记忆有用"，
SCORE / DOME 也能证明；只有这个实验能证明"**分支隔离**有用"，而那是本文独有的贡献。

### 4-2 其余工作

| # | 工作 | 目的 | 负责 |
|---|---|---|---|
| 1 | 扩样至 ≥15 故事，每配置重复 3 轮 | 降低方差，使统计检验可信（当前 n=5 全不显著） | 肖文宇 |
| 2 | 接入 LLM-as-Judge 四维打分 | 补足语义层一致性指标（trigram 测不出因果与性格漂移） | 黄夏薇 |
| 3 | 人工阅读打分与自动指标交叉验证 | 验证指标本身是否有效 | 全员 |
| 4 | 图文一致性实验（appearance 约束前后对比） | 扩展"一致性"到跨模态 | 章真毓 |

> **关于 4-2 的第 4 项**：生图链路的代码修复已完成并有单测覆盖
> （`tests/image-seed.test.ts` 16 项断言，含"多一个角色 seed 不变"的回归用例），
> 但**尚未真正生成过图片验证** —— 因为 `.env` 里 `AI_IMAGE_API_KEY` 仍是占位符
> `your-a...`。需先配置可用的图片 API Key，才能跑通端到端对比。
